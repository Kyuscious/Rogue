services:
  - type: web
    name: runeterrogue
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
